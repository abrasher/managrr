mutation updateUsers($data: [UpdateUserSharingInput!]!) {
  updateUsers(data: $data) {
    id
    sharedServers {
      id
      machineIdentifier
      allLibraries
      libraries {
        id
      }
    }
  }
}
