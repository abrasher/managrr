mutation updateUsers($input: [UpdateUserSharingInput!]!) {
  updateUsers(input: $input) {
    id
    title
    sharedServers {
      id
      machineIdentifier
      allLibraries
      libraries {
        id
      }
    }
  }
}
