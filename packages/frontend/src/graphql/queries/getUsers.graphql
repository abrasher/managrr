query getUsers {
  account {
    servers {
      id
      clientIdentifier
      name
      libraries {
        id
        title
        type
      }
    }
    users {
      ...userFields
    }
  }
}
